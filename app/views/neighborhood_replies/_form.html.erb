<%= form_with(model: neighborhood_reply, local: true) do |form| %>
  <% if neighborhood_reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(neighborhood_reply.errors.count, "error") %> prohibited this neighborhood_reply from being saved:</h2>

      <ul>
      <% neighborhood_reply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reply %>
    <%= form.text_field :reply %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :video_url %>
    <%= form.text_field :video_url %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :neighborhood_post_id %>
    <%= form.text_field :neighborhood_post_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
